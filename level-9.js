
let rainbow = ["red", "orange", "yellow", "green", "blue", "indigo", "violet"] 

for (let i = 0; i < rainbow.length; i++) {
    // whatever is the last place is the thing that runs last in the loop
    // the for loop just checks what the current value is and then adds 1 to it at the end so when the next time it checks the condition 
    if (i % 2 == 0) {
        console.log(rainbow[i])
        console.log(rainbow[i])
    } else {
        console.log(rainbow[i])
    }
   
}